# LaTeX build artifacts — regenerated by latexmk / pdflatex / xelatex.
# LaTeX 编译产物——由 latexmk / pdflatex / xelatex 再生。
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.pdf

# Python cache · Python 缓存
__pycache__/
*.pyc

# OS junk · 系统垃圾文件
.DS_Store
Thumbs.db

# Committed figures are NOT ignored — paper/{en,zh}/ stay self-contained.
# 已提交的配图不在忽略之列——paper/{en,zh}/ 保持自包含。
